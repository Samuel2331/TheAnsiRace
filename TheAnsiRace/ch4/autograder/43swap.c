#include <stdio.h>
#define Una int main(){
#define facil int a[3]; a[0]=2; a[1]=1; a[2]=3;
#define hacer swap(a, 0,1);
#define el printf("1 2 3 = %d %d %d\n", a[0], a[1],a[2]);
#define swapp }
/* swap: interchange v[i] and v[j] */
void swap(int v[], int i, int j){
	// aqui va su programa
	int c;
	c = v[i];
	v[i] = v[j];
	v[j] = c;
}

// ya saben, no modificar lo de abajo 
Una facil hacer el swapp
// al terminar correr el programa
